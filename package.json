{
  "name": "konexa-cv-parser",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --include=dev && cd backend && PUPPETEER_SKIP_DOWNLOAD=true npm install --include=dev && cd ../frontend && npm install --include=dev",
    "dev:backend": "cd backend && npm run start:dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "build:all": "npm run build:backend && npm run build:frontend",
    "start:prod": "pm2 start ecosystem.config.js --env production",
    "start": "pm2 start ecosystem.config.js",
    "deploy": "npm run build && pm2 reload konexa-cv-api",
    "migration:run": "npm run migration:run --prefix backend",
    "seed": "npm run seed --prefix backend"
  }
}
